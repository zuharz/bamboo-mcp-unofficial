# =============================================================================
# BambooHR MCP Server Configuration
# Copy this file to .env and fill in your actual values
# =============================================================================

# Required: BambooHR API Credentials
# Get your API key from: https://[subdomain].bamboohr.com/settings/api.php
BAMBOO_API_KEY=your_bamboo_api_key_here
BAMBOO_SUBDOMAIN=your_company_subdomain

# =============================================================================
# Development & Testing Configuration
# =============================================================================

# Environment (development, test, production)
NODE_ENV=development

# Logging level (debug, info, warn, error, silent)
# Use 'debug' for development, 'info' for production
LOG_LEVEL=info

# =============================================================================
# Integration Testing
# =============================================================================
# Integration tests use the same BAMBOO_API_KEY and BAMBOO_SUBDOMAIN above
# Tests will automatically skip if credentials are missing

# Test timeout in milliseconds
TEST_TIMEOUT=10000

# =============================================================================
# Optional: Advanced Configuration
# =============================================================================

# HTTP client cache timeout (5 minutes = 300000ms)
# CACHE_TIMEOUT_MS=300000

# API request timeout (30 seconds = 30000ms)
# API_TIMEOUT_MS=30000

# =============================================================================
# Setup Instructions:
# =============================================================================
# 1. Copy this file: cp .env.example .env
# 2. Get your API key from BambooHR Settings > API Keys
# 3. Find your subdomain in your BambooHR URL (e.g., 'company' from company.bamboohr.com)
# 4. Edit .env with your actual credentials
# 5. Run integration tests: npm test
# 6. Build and start: ./scripts/build.sh && node dist/bamboo-mcp.js